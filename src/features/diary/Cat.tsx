// React Imports
import React from 'react';
// Routers Imports
import { Outlet } from 'react-router-dom';

export const Cat = () => {
    return (
        <div>
            <Outlet />
        </div>
    )
}